@model RecordStore.ViewModels.ArtistDropdownViewModel

@foreach (var artist in Model.Artists)
{
    <option value="@artist.Id" selected="@(artist.Id == Model.SelectedArtistId)">@artist.Name</option>
}
